import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';
import { Providers } from '@/providers/Providers';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: {
    default: 'Savory Bites - Premium Restaurant Experience',
    template: '%s | Savory Bites',
  },
  description:
    'Experience premium dining with Savory Bites. Fresh ingredients, exceptional flavors, delivered to your door.',
  keywords: ['restaurant', 'food', 'dining', 'premium', 'delivery'],
  openGraph: {
    title: 'Savory Bites - Premium Restaurant Experience',
    description: 'Fresh ingredients, exceptional flavors, delivered to your door.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>
        <Providers>
          <div className="flex min-h-screen flex-col">
            <Header />
            <main className="flex-1">{children}</main>
            <Footer />
          </div>
        </Providers>
      </body>
    </html>
  );
}
